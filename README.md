docker build .

docker build -t visits .

docker run visits

Another Terminal

docker run redis

########### Final command #############

docker-compose up

docker-compose up --build

### running in background

docker-compose up -d

docker-compose down
